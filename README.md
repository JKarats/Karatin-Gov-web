# Karatin Mobile Fix

Upload the `css` folder to the repository root and replace the existing
`css/style.css`.

This fixes:
- Introduction Letter mobile overlap
- Document-card image/icon overlap
- Mobile heading wrapping
- Mobile button stacking
- Sidebar/logo sizing
